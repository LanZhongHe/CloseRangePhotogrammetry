# 人工控制点自动/半自动识别系统 — 开发设计文档

> 版本: v0.4 | 日期: 2026-04-14 | 状态: 需求确认完毕，可进入开发

---

## 1. 背景与目标

在近景摄影测量中，人工控制点（Target）是用于建立物方坐标系与像方坐标系之间映射关系的关键要素。本系统旨在从输入影像中自动或半自动识别并定位人工控制点的**像方坐标（像素坐标）**，为后续的单片空间后方交会（space resection）提供精确的控制点影像数据。

### 核心目标

1. 从单张影像中自动检测人工控制点（同心圆环+十字划线样式）
2. 以高亚像素精度定位控制点中心坐标
3. 支持仿射变形场景下的稳健识别
4. 输出结构化的控制点数据，可直接用于后方交会解算

---

## 2. 已确认的系统参数

| 参数 | 确定值 |
|------|--------|
| **控制点样式** | 同心圆环（1层）+ 十字划线，下方附数字编号 |
| **控制点颜色** | 白底黑环 |
| **影像分辨率** | 约 8500 × 5000 像素 |
| **控制点像素尺寸** | 约 200 × 250 像素 |
| **光照条件** | 人工光照，无需反光材料额外亮度增强 |
| **亚像素精度要求** | 尽可能高（目标 ≤ 0.05px） |
| **单张影像控制点数量** | 较多 |
| **编号数字** | 字体、大小固定，范围 001 ~ 999（三位数） |
| **编程语言** | Python |
| **输出格式** | JSON |
| **处理方式** | 批量处理多张影像 |
| **界面需求** | 需要 GUI 界面 |
| **畸变校正** | 不在检测前做畸变校正 |

---

## 3. 控制点样式定义

### 3.1 样式描述

本系统的目标控制点样式为**白底 + 单层黑环 + 十字划线 + 数字编号**，结构如下：

```
          ┌───────────────────┐
          │                   │
          │      ╭───╮        │
          │     ╱  │  ╲       │
          │    │ ──┼── │      │    黑色圆环（单层）
          │    │   │   │      │    十字划线（细线，贯穿圆心）
          │     ╲  │  ╱       │
          │      ╰───╯        │
          │                   │
          │       01          │    数字编号（白底黑字）
          │                   │
          └───────────────────┘
```

- **黑色圆环（单层）**：白底上的黑色环形，是粗检测的主要特征
- **十字划线**：穿过圆心的十字丝，黑色细线，用于高精度中心定位
- **数字编号**：位于靶标下方，字体大小固定，用于关联物方坐标，系统需识别并忽略

### 3.2 仿射变形的影响

由于相机视角倾斜，圆形控制点在影像中会呈现为**椭圆**。系统需要：

- 使用椭圆拟合代替圆拟合，适应仿射变形
- 通过椭圆的长短轴比评估变形程度，设置合理阈值
- 控制点尺寸为 200×250 px，采样充足，亚像素定位条件良好
- 对严重变形（如倾斜角度 > 60°）的控制点进行降权或标记

---

## 4. 功能模块设计

### 4.1 模块总览

```
┌──────────────────────────────────────────────────────────────┐
│                       控制点识别系统                            │
├──────────┬──────────────┬────────────┬──────────┬────────────┤
│ 影像预处理 │  粗检测定位    │ 精确定位   │ 编号识别  │ 数据输出   │
└──────────┴──────────────┴────────────┴──────────┴────────────┘
                                          ↕ 半自动交互精化
```

### 4.2 模块一：影像预处理

8500×5000 的大分辨率影像，需要自适应处理策略：

| 功能 | 说明 | 技术要点 |
|------|------|----------|
| 影像读取 | 支持 JPEG, PNG, TIFF, BMP | 大图分块读取避免内存溢出 |
| 灰度转换 | 彩色转灰度 | — |
| 对比度增强 | CLAHE 自适应直方图均衡化 | `clipLimit` 自适应，针对大图分块处理 |
| 去噪 | 双边滤波（保边去噪） | 保留圆环边缘清晰度 |
| 二值化 | 自适应阈值（THRESH_BINARY_INV） | 白底黑环 → 反向阈值使环形区域变为前景 |

**自适应策略**：

- 根据影像分辨率与已知控制点像素尺寸（~200×250 px）自动计算滤波核大小和阈值参数
- CLAHE 的 `clipLimit` 和 `tileGridSize` 根据影像尺寸动态调整
- 二值化 block size 建议设为控制点直径的 1.5~2 倍（约 300~400 px）
- 使用 `THRESH_BINARY_INV`：白底黑环场景下，使黑色环形区域变为白色（前景），便于轮廓提取

### 4.3 模块二：粗检测 — 候选区域提取

#### 4.3.1 基于外圆环轮廓的候选检测

由于控制点尺寸较大（~200×250 px），在 8500×5000 的影像中特征显著，适合基于轮廓的检测：

1. **轮廓提取**：对二值化图像（反向阈值后）使用 `cv2.findContours` 提取连通区域
2. **几何筛选**（各参数根据控制点尺寸 200~250 px 自适应设置）：
   - 面积范围：控制点圆环面积的 ±50%（自适应计算）
   - 圆度筛选：`circularity = 4π × Area / Perimeter²`，阈值 > 0.7（放宽以适应仿射变形）
   - 长宽比筛选：外接矩形长宽比在 0.6 ~ 1.7 范围内（适应仿射变形）
   - 凸性筛选：凸包面积比 > 0.9
3. **环形结构验证**：白底黑环在反向二值化后表现为一个环形连通区域。验证候选轮廓的内部应包含一个空洞（环形），使用 `cv2.RETR_TREE` 或内外轮廓面积比进行验证
4. **编号区域排除**：数字编号位于圆环下方，通过以下策略排除：
   - 在每个候选圆环中心的下方固定偏移范围内检测是否有编号候选区域
   - 编号区域面积远小于圆环区域，且形状为窄长矩形
   - 被判定为编号的区域从圆环候选列表中移除

#### 4.3.2 候选方案：霍夫圆检测

如果轮廓法在复杂场景中效果不佳，可作为备选：

```python
circles = cv2.HoughCircles(
    gray, cv2.HOUGH_GRADIENT, dp=1, minDist=200,
    param1=50, param2=30,
    minRadius=80, maxRadius=130  # 根据控制点尺寸自适应
)
```

### 4.4 模块三：精确定位 — 亚像素中心坐标

这是本系统的核心精度环节。控制点 200×250 px 的尺寸为高精度定位提供了良好的采样条件。

#### 4.4.1 算法策略：多方法融合

采用**由粗到精**的多级定位策略，目标精度 ≤ 0.05px：

**第一级：椭圆拟合（精度 ~0.1px）**

对圆环的内外边缘轮廓分别进行椭圆拟合，取多个拟合中心的加权平均：

```python
# 对同心圆环的各层轮廓分别拟合
outer_ellipse = cv2.fitEllipse(outer_contour)    # 外环边缘
inner_ellipse = cv2.fitEllipse(inner_contour)    # 内环边缘

# 加权平均（可根据轮廓质量调整权重）
center_x = (outer_center_x * w1 + inner_center_x * w2) / (w1 + w2)
center_y = (outer_center_y * w1 + inner_center_y * w2) / (w1 + w2)
```

**第二级：十字划线交点精化（精度 ~0.05px）**

利用控制点自带的十字划线进行高精度定位：

1. 在椭圆拟合中心附近的 ROI 内，使用 Steger 算法（基于 Hessian 矩阵的线检测）提取十字划线的亚像素线位置
2. 对水平线和垂直线（考虑旋转变形）分别拟合直线方程
3. 两线交点即为控制点中心

```python
# Steger 线检测核心思路
from scipy.ndimage import gaussian_filter

# 计算 Hessian 矩阵
Ix = cv2.Sobel(roi, cv2.CV_64F, 1, 0, ksize=5)
Iy = cv2.Sobel(roi, cv2.CV_64F, 0, 1, ksize=5)
Ixx = cv2.Sobel(Ix, cv2.CV_64F, 1, 0, ksize=5)
Iyy = cv2.Sobel(Iy, cv2.CV_64F, 0, 1, ksize=5)
Ixy = cv2.Sobel(Ix, cv2.CV_64F, 0, 1, ksize=5)

# 在每个像素处求 Hessian 矩阵最大特征值对应的特征向量
# 沿特征向量方向进行抛物线插值 → 亚像素边缘位置
```

**第三级：灰度加权中心精化（精度 ~0.02px，可选）**

在最终中心位置的小邻域内，使用灰度加权质心法进一步精化：

```python
roi = img[cy-r:cy+r, cx-r:cx+r]
# 取反（目标为暗区）后计算加权质心
weights = (255 - roi).astype(np.float64)
yy, xx = np.mgrid[0:roi.shape[0], 0:roi.shape[1]]
fine_cx = np.sum(xx * weights) / np.sum(weights) + (cx - r)
fine_cy = np.sum(yy * weights) / np.sum(weights) + (cy - r)
```

#### 4.4.2 各级方法对比

| 方法 | 理论精度 | 适用靶标特征 | 在本系统中的角色 |
|------|----------|-------------|----------------|
| 椭圆拟合 | ~0.1 px | 圆环边缘 | 粗定位 + 环形靶标配准 |
| Steger 十字交点 | ~0.05 px | 十字划线 | **主要精定位方法** |
| 灰度加权质心 | ~0.02 px | 高对比度靶标 | 最终精化（可选） |
| 高斯曲面拟合 | ~0.05 px | 实心圆点 | 不适用于环形靶标，备选 |

### 4.5 模块四：编号识别

控制点下方附有数字编号（字体大小固定），需要识别以关联物方坐标：

| 功能 | 说明 |
|------|------|
| 编号区域定位 | 在检测到的圆环中心下方固定偏移处裁剪编号 ROI |
| 模板匹配识别 | 利用已知字体大小制作数字模板，逐字符匹配 |
| 编号绑定 | 将识别结果绑定到对应的 `TargetPoint` 对象 |

**技术方案**：

由于字体和大小固定，**模板匹配**是首选方案（准确率高、鲁棒性好）：

1. 根据已知字体生成 0-9 单字符模板图像
2. 在编号 ROI 内使用 `cv2.matchTemplate` 逐字符匹配
3. 编号固定为三位数字（001~999），按三个字符等宽分割 ROI 进行匹配
4. 若模板匹配置信度低于阈值，回退到 GUI 手动输入

备选：Tesseract OCR（`pytesseract`），适用于编号格式不固定的扩展场景

### 4.6 模块五：半自动交互精化

当自动检测结果不理想时，提供人工介入机制：

| 功能 | 说明 |
|------|------|
| **手动添加** | 用户在影像上点击大致位置，系统在局部区域搜索并精确拟合 |
| **手动删除** | 移除误检点 |
| **ROI 框选** | 用户划定感兴趣区域，仅在该区域内检测 |
| **参数调节面板** | 实时调整检测参数（面积范围、圆度阈值等），即时预览 |
| **结果可视化** | 在原图上叠加显示检测到的控制点、编号及定位精度信息 |
| **编号修正** | 手动修正 OCR 识别错误的编号 |

### 4.7 模块六：数据输出与接口

#### 4.7.1 输出数据结构

```json
{
  "image": "IMG_0001.jpg",
  "image_size": [8500, 5000],
  "detection_time": "2026-04-14T10:30:00",
  "targets": [
    {
      "id": "01",
      "pixel_x": 1523.4567,
      "pixel_y": 876.2345,
      "confidence": 0.97,
      "source": "auto",
      "ellipse": {
        "semi_major": 118.3,
        "semi_minor": 112.8,
        "angle_deg": 15.2
      },
      "eccentricity": 0.30,
      "subpixel_method": "steger_cross"
    }
  ]
}
```

#### 4.7.2 为后方交会预留的接口

后方交会需要的输入数据包括：

- **像点坐标**：本系统直接输出（`pixel_x`, `pixel_y`）
- **对应的物方坐标**：通过编号识别自动关联或手动输入
- **影像内方位元素**（焦距、主点）：外部输入

因此系统需要预留：

```
┌──────────────────┐      ┌──────────────────────┐
│ 控制点检测模块      │ ──→ │ 像点坐标 (u, v)       │
│ (本系统)          │      │ + 编号 → 物方坐标映射   │
└──────────────────┘      └──────────┬───────────┘
                                     ▼
                            ┌──────────────────┐
                            │ 后方交会模块       │
                            │ (后续开发)         │
                            │ 物方坐标 + 内方位元素│
                            └──────────────────┘
```

`TargetPoint` 数据类设计：

```python
from dataclasses import dataclass, field
from typing import Optional, Tuple

@dataclass
class EllipseInfo:
    """椭圆拟合参数"""
    semi_major: float          # 长半轴 (px)
    semi_minor: float          # 短半轴 (px)
    angle_deg: float           # 旋转角度 (度)

@dataclass
class TargetPoint:
    """单个控制点的数据结构"""
    id: str                                # 控制点编号（如 "01"）
    pixel_x: float                         # 像方 x 坐标（像素，亚像素精度）
    pixel_y: float                         # 像方 y 坐标（像素，亚像素精度）
    obj_x: Optional[float] = None          # 物方 X 坐标（待绑定）
    obj_y: Optional[float] = None          # 物方 Y 坐标（待绑定）
    obj_z: Optional[float] = None          # 物方 Z 坐标（待绑定）
    confidence: float = 1.0                # 检测置信度 [0, 1]
    source: str = "auto"                   # 来源: "auto" / "manual"
    subpixel_method: str = ""              # 定位方法: "ellipse" / "steger_cross" / "centroid"
    ellipse: Optional[EllipseInfo] = None  # 外环椭圆参数
    eccentricity: Optional[float] = None   # 离心率（评估变形程度）
```

### 4.8 模块七：GUI 界面

需要提供图形用户界面，方便批量操作和交互修正。

#### 4.8.1 GUI 技术选型

| 方案 | 优势 | 劣势 |
|------|------|------|
| **PyQt5** | 性能好、大图支持好、控件丰富 | 学习曲线较陡 |
| Gradio | 快速搭建、基于 Web | 大图显示性能差 |
| Tkinter | 内置、轻量 | 控件有限、大图卡顿 |

**推荐 PyQt5**：8500×5000 大图需要高效的图像显示和缩放，PyQt5 + `QGraphicsView` 可提供流畅的缩放平移体验。

#### 4.8.2 GUI 功能布局

```
┌──────────────────────────────────────────────────────────────────┐
│  菜单栏：文件 | 检测 | 工具 | 帮助                                  │
├────────────┬──────────────────────────────────┬──────────────────┤
│            │                                  │                  │
│  文件列表    │     影像显示区                     │   属性/参数面板   │
│            │     (QGraphicsView 大图浏览)       │                  │
│  [IMG_001] │                                  │  检测参数:       │
│  [IMG_002] │     + 控制点叠加标注               │  - 面积范围      │
│  [IMG_003] │     + 编号标签                    │  - 圆度阈值      │
│  [  ...  ] │     + 十字线交点标记               │  - 长宽比范围    │
│            │                                  │                  │
│            │                                  │  当前选中控制点:   │
│            │                                  │  编号: 01        │
│            │                                  │  坐标: (x, y)    │
│            │                                  │  置信度: 0.97    │
│            │                                  │  [修正编号]      │
│            │                                  │  [删除]          │
├────────────┴──────────────────────────────────┴──────────────────┤
│  状态栏：已检测 12 个控制点 | 当前影像 1/5 | 精度: 0.03px          │
└──────────────────────────────────────────────────────────────────┘
```

#### 4.8.3 批量处理流程

```
用户操作:  选择文件夹 → 加载影像列表 → 点击"全部检测"
系统流程:  遍历影像 → 逐张检测 → 进度条更新 → 汇总结果
用户操作:  在列表中逐张检查 → 修正误检/漏检 → 导出全部结果
```

- 进度条显示当前处理的影像编号和总数
- 检测完成后自动切换到第一张影像显示结果
- 支持批量导出所有影像的检测结果为一个汇总 JSON 文件

---

## 5. 技术选型

### 5.1 核心库

| 组件 | 方案 | 说明 |
|------|------|------|
| 语言 | Python 3.10+ | — |
| 图像处理 | OpenCV (`cv2`) | 轮廓检测、椭圆拟合、形态学操作 |
| 数值计算 | NumPy | 矩阵运算、亚像素插值 |
| 科学计算 | SciPy | Hessian 矩阵特征值求解（Steger 线检测） |
| 可视化 | Matplotlib | 大图分块显示、结果叠加 |
| OCR（可选） | 模板匹配（首选）/ Tesseract | 编号识别，字体固定时模板匹配精度更高 |
| GUI | PyQt5 | 大图浏览、批量处理、交互修正 |

### 5.2 项目结构

```
CloseRangePhotogrammetry/
├── ControlPoint_Detection_Design.md   ← 本文件
├── requirements.txt
├── src/
│   ├── __init__.py
│   ├── preprocessing.py       # 影像预处理（自适应 CLAHE、二值化）
│   ├── detection.py           # 粗检测：轮廓筛选、环形验证、编号排除
│   ├── subpixel.py            # 精确定位：椭圆拟合、Steger 十字线、灰度加权
│   ├── id_recognition.py      # 编号识别（模板匹配）
│   ├── interaction.py         # 半自动交互逻辑
│   ├── data_model.py          # TargetPoint、EllipseInfo 数据结构
│   └── io_utils.py            # JSON 导出
├── gui/
│   ├── __init__.py
│   ├── main_window.py         # 主窗口（QGraphicsView 大图浏览）
│   ├── batch_panel.py         # 批量处理面板
│   └── param_panel.py         # 检测参数调节面板
├── templates/                 # 编号数字模板图像
│   └── digits/
│       ├── 0.png
│       └── ...
├── tests/
│   ├── test_detection.py
│   ├── test_subpixel.py
│   └── test_data/             # 测试影像
└── output/                    # 检测结果输出（JSON）
```

---

## 6. 待明确需求清单

以下问题可在开发过程中逐步确认：

| # | 问题 | 影响范围 |
|---|------|----------|
| 1 | 十字划线的宽度（像素），是否贯穿整个圆环 | Steger 线检测的 sigma 参数 |
| 2 | 圆环的内外径比例（环的粗细） | 轮廓面积筛选参数 |
| 3 | 控制点之间是否有最小间距 | 影像中是否有密集排列场景 |

---

## 7. 开发阶段规划

### 第一阶段：核心算法验证

- [ ] 实现自适应影像预处理（白底黑环、反向阈值、8500×5000 大图）
- [ ] 实现单层黑环控制点的粗检测（轮廓筛选 + 环形验证 + 编号排除）
- [ ] 实现椭圆拟合亚像素定位
- [ ] 实现 Steger 十字划线交点精化定位
- [ ] 实现检测结果可视化（在原图上叠加显示）
- [ ] 准备测试数据集，验证检测精度

### 第二阶段：编号识别与数据输出

- [ ] 生成数字模板（0-9），实现模板匹配编号识别
- [ ] 实现 `TargetPoint` 数据结构与 JSON 导出
- [ ] 编号与物方坐标的映射接口

### 第三阶段：GUI 界面

- [ ] PyQt5 主窗口搭建（QGraphicsView 大图浏览与缩放平移）
- [ ] 文件列表 + 批量处理进度条
- [ ] 控制点叠加标注与可视化
- [ ] 参数调节面板（面积、圆度等实时预览）
- [ ] 手动添加/删除控制点、编号修正
- [ ] 批量导出 JSON

### 第四阶段：后方交会对接（后续）

- [ ] 后方交会模块对接
- [ ] 畸变校正预处理（按需）

---

## 8. 风险与注意事项

1. **大分辨率内存问题**：8500×5000 影像单通道 uint8 约 42MB，灰度+二值化+多级处理可能需要 200MB+。PyQt5 显示区需使用 `QPixmap` 切片加载
2. **光照不均**：人工光照下可能存在阴影和高光。CLAHE 分块处理可缓解，但极端情况需要分区域自适应阈值
3. **编号干扰检测**：数字编号可能被误识别为环形区域的一部分。需要在检测阶段通过几何约束（位置在圆环下方、面积/形状不同）排除编号区域
4. **十字划线与圆环边缘的干扰**：Steger 线检测时十字划线与圆环边缘交叉，需要在 ROI 内排除圆环边缘的影响（可使用形态学开运算分离）
5. **严重遮挡或截断**：部分控制点可能被遮挡或截断，椭圆拟合对部分遮挡有鲁棒性，但截断严重时需要标记为低置信度
6. **镜头畸变**：不在检测前做畸变校正，边缘区域的控制点坐标会存在系统偏差。此偏差需在后方交会阶段处理，或后续按需添加畸变校正预处理
7. **精度验证**：需要已知真值（如高精度全站仪测量值）来评估亚像素定位精度
8. **批量处理性能**：单张 8500×5000 影像检测预计需数秒，多张批量处理时需考虑进度反馈和后台线程避免 GUI 卡顿

---

## 附录：自适应参数计算

为方便 8500×5000 分辨率和 ~200×250 px 控制点的自适应处理，关键参数的计算逻辑：

```python
# 基于已知控制点像素尺寸的自适应参数
TARGET_SIZE_PX = 200  # 控制点大致像素尺寸（取短轴）

# 预处理参数
CLAHE_TILE_SIZE = max(8, TARGET_SIZE_PX // 25)   # ~8
BILATERAL_D = TARGET_SIZE_PX // 20                # ~10
ADAPTIVE_BLOCK = int(TARGET_SIZE_PX * 1.5) | 1   # ~301（奇数）

# 检测参数（不考虑内外半径比，基于整体靶标面积）
AREA_EXPECTED = 3.14 * (TARGET_SIZE_PX / 2) ** 2  # 靶标外接圆面积
AREA_MIN = AREA_EXPECTED * 0.5                      # ±50%容差
AREA_MAX = AREA_EXPECTED * 1.5
MIN_CONTOUR_POINTS = 50                             # 最小轮廓点数（保证椭圆拟合质量）

# 精定位 ROI
ROI_SIZE = int(TARGET_SIZE_PX * 0.4)              # 十字线搜索 ROI（~80px）
```
